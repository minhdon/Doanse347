# --- Web Framework & Server ---
Flask==3.1.1
flask-cors==6.0.2
gunicorn==23.0.0
requests==2.32.3
uvicorn  # Cần thêm nếu bạn dùng asgi, nếu không thì bỏ
python-dotenv  # Thường cần để đọc file .env

# --- AI & Data Processing (Giữ lại những cái chính) ---
google-generativeai==0.8.6
numpy==2.2.6
pandas==2.2.3
pillow==10.4.0
scikit-learn==1.6.1
scipy==1.13.1
ultralytics==8.3.169
spacy==3.8.5
vncorenlp==1.0.3
opencv-python-headless==4.10.0.84  # Đổi sang bản headless để chạy trên server không cần màn hình

# --- Hỗ trợ AI (Torch - Giữ lại nếu bạn dùng YOLO/PyTorch) ---
# Lưu ý: Torch rất nặng, nếu Render báo OOM (hết Ram) thì cần xem xét lại
torch==2.6.0
torchvision==0.21.0+cpu

# --- Các thư viện tiện ích khác ---
beautifulsoup4==4.12.3
openpyxl==3.1.5
python-docx==1.1.2
PyYAML==6.0.2
tqdm==4.67.1
urllib3==2.2.3
Werkzeug==3.1.3
setuptools
wheel